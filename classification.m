%% preprocessing
all_data = import_data('transfusion.data');
samples = all_data(:,1:4);
reference = all_data(:,5);

% hold on;
% grid on;
% plot(samples{:,1}, reference{:,1}, 'rx');

%% processing


%% postprocessing
